---
title: 'Big Walk Connection Failed or Join Code Not Working'
date: '2026-08-05'
lastmod: '2026-08-14'
summary: 'Fix an invalid Join Code, failed session connection, platform mismatch or stale host session.'
category: 'Technical Help'
aliases: ["can't join friend", 'code not working', 'connection issue']
quickAnswer: 'Ask the host for a fresh code from the active session, confirm both players are updated, and recreate the lobby if the code belongs to an ended session.'
tags: ['help', 'connection', 'join code']
images: ['/static/images/big-walk/official-01.jpg']
layout: GuideLayout
---

## Fast checklist

<GameImage
  src="/static/images/big-walk/official-05.jpg"
  alt="A Big Walk group gathered together before starting a shared session"
  caption="Connection troubleshooting is fastest when the whole group checks the same lobby, version and host at the same time. Official screenshot."
/>

1. Copy the Join Code again and remove spaces before or after it.
2. Confirm the host is still inside the session that generated the code.
3. Make sure every player is running the same game version.
4. Check that crossplay is enabled when platforms differ.
5. Have the host recreate the session and send a fresh code.
6. Restart the game and network connection if a fresh lobby also fails.

## Remember who owns progress

The host stores the group progress. If you recreate a continuing session, the same person should host it so the group returns to the correct save.

## Before reporting the issue

Note the host platform, joining platform, error text, time and whether a new code changed anything. This makes a support report much more useful.

## One easy-to-miss cause

Check that every device's system clock is accurate. House House says a clock that differs significantly from online time can prevent both joining and hosting.

## Sources checked

- [Official Big Walk FAQ](https://bigwalk.game/faq/) — connection, version and system-clock troubleshooting
